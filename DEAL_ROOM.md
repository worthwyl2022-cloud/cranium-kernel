# Deal room — Cranium Kernel

Operator: Wyl Mathes
Asset: `worthwyl2022-cloud/cranium-kernel`
Posture: evaluation now; exclusive license or assignment only by signed instrument.

## In the offered tree

- TypeScript kernel: `src/kernel/`
- Governance adapters: `src/governance/`
- Miracle Memory: `src/memory/`
- Kotlin verification twin: `app/` (not the second product)
- Lab UI: `src/App.tsx` and `src/components/` (demo, not the library API)
- Evidence commands: `EVIDENCE.md`

## Out of the offered tree

- Other public repos under worthwyl2022-cloud unless added by written amendment
- `cranium-relics` (private archive, provenance only)
- Production key custody, HSM, rotation, revocation
- Any claim of uniqueness or world-first status

## Source of truth

`DefaultAuthorityTransitionEngine` evaluates. `KernelStateReducer` commits.
`GovernedKernelPort` does not issue authority.

Kotlin package `com.example.cranium` is a known defect. Do not present it
as the shipping namespace. Rename or drop it before a priced term sheet.
